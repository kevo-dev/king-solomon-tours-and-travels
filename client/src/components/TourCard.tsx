import { ArrowUpRight, Clock3, Heart, MapPin, Users } from "lucide-react";
import { Tour, formatKes } from "@/lib/travelData";
import { useLocation } from "wouter";

export default function TourCard({ tour, onSave }: { tour: Tour; onSave?: (id: number) => void }) {
  const [, setLocation] = useLocation();
  return <article className="group overflow-hidden rounded-[1.5rem] bg-[#fffdf8] shadow-[0_15px_45px_rgba(63,48,27,0.1)] transition duration-300 hover:-translate-y-1 hover:shadow-[0_22px_55px_rgba(63,48,27,0.16)]"><div className="relative aspect-[4/3] overflow-hidden"><img src={tour.image} alt={`${tour.destination} travel experience`} className="h-full w-full object-cover transition duration-700 group-hover:scale-105" /><button onClick={(event) => { event.stopPropagation(); onSave?.(tour.id); }} className="absolute right-3 top-3 grid size-9 place-items-center rounded-full bg-white/90 text-[#263e34] backdrop-blur transition hover:bg-white" aria-label={`Save ${tour.title}`}><Heart className="size-4" /></button><span className="absolute bottom-3 left-3 rounded-full bg-[#17352c]/90 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[.14em] text-white backdrop-blur">{tour.category}</span></div><button onClick={() => setLocation(`/tours/${tour.slug}`)} className="block w-full p-5 text-left"><div className="mb-3 flex items-center gap-1.5 text-xs font-medium text-[#967142]"><MapPin className="size-3.5" />{tour.destination}</div><h3 className="font-display text-2xl leading-tight text-[#243b31]">{tour.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-[#6b6255]">{tour.summary}</p><div className="mt-4 flex items-center gap-3 border-t border-[#efe6d6] pt-4 text-xs text-[#6f695e]"><span className="flex items-center gap-1"><Clock3 className="size-3.5" />{tour.days} {tour.days === 1 ? "day" : "days"}</span><span className="flex items-center gap-1"><Users className="size-3.5" />{tour.group}</span></div><div className="mt-4 flex items-end justify-between"><p className="text-lg font-bold text-[#213a30]">{formatKes(tour.price)} <span className="text-xs font-normal text-[#807667]">per person</span></p><span className="grid size-8 place-items-center rounded-full bg-[#e9f0e7] text-[#315b49] transition group-hover:bg-[#315b49] group-hover:text-white"><ArrowUpRight className="size-4" /></span></div></button></article>;
}
