import TravelNav from "@/components/TravelNav";
import { formatKes, tours } from "@/lib/travelData";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, CalendarDays, Check, ChevronRight, Clock3, Heart, MapPin, MessageCircle, ShieldCheck, Users, X } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "sonner";
import { useLocation, useRoute } from "wouter";

export default function TourDetail() {
  const [, params] = useRoute("/tours/:slug"); const [, setLocation] = useLocation(); const tour = tours.find((item) => item.slug === params?.slug) ?? tours[0];
  const [form, setForm] = useState({ name: "", email: "", travelers: "2", date: "", message: "" });
  const inquiry = trpc.travel.inquire.useMutation({ onSuccess: () => { toast("Your inquiry is on its way. The operator can now follow up with next steps."); setForm({ name: "", email: "", travelers: "2", date: "", message: "" }); }, onError: () => toast("We couldn’t send that inquiry just now. Please try again.") });
  const submit = (event: FormEvent) => { event.preventDefault(); inquiry.mutate({ tourId: tour.id, travelerName: form.name, travelerEmail: form.email, travelerCount: Number(form.travelers), travelDate: form.date ? new Date(form.date) : undefined, message: form.message || undefined }); };
  return <div className="min-h-screen bg-[#f8f3e8]"><TravelNav /><main><div className="container pt-6"><button onClick={() => setLocation("/tours")} className="flex items-center gap-2 text-sm font-bold text-[#315b49]"><ArrowLeft className="size-4" />Back to explore</button></div><section className="container py-6 md:py-10"><div className="grid gap-7 lg:grid-cols-[1.5fr_.8fr]"><div><div className="relative overflow-hidden rounded-[2rem] bg-[#17352c]"><img src={tour.image} alt={`${tour.destination} experience`} className="aspect-[16/10] h-full w-full object-cover" /><span className="absolute left-5 top-5 rounded-full bg-white/90 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[.15em] text-[#315b49]">{tour.category}</span><button onClick={() => toast(`${tour.title} added to your wishlist.`)} className="absolute right-5 top-5 grid size-10 place-items-center rounded-full bg-white/90 text-[#263e34]"><Heart className="size-4" /></button></div><section className="mt-4"><div className="flex items-center justify-between"><p className="eyebrow">A sense of place</p><p className="text-xs text-[#7c7265]">Photo gallery</p></div><div className="mt-3 grid grid-cols-3 gap-3">{tour.gallery.map((image, index) => <img key={image} src={image} alt={`${tour.destination} gallery image ${index + 1}`} className="aspect-[4/3] rounded-2xl object-cover" />)}</div></section><div className="mt-7"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.14em] text-[#a07136]"><MapPin className="size-3.5" />{tour.destination}, {tour.region}</div><h1 className="mt-3 font-display text-5xl leading-[1.02] text-[#263e34] md:text-6xl">{tour.title}</h1><p className="mt-5 max-w-2xl text-base leading-7 text-[#655c50]">{tour.summary}</p><div className="mt-6 flex flex-wrap gap-5 border-y border-[#e4dac8] py-5 text-sm text-[#5f584d]"><span className="flex items-center gap-2"><Clock3 className="size-4 text-[#315b49]" />{tour.days} days</span><span className="flex items-center gap-2"><Users className="size-4 text-[#315b49]" />{tour.group}</span><span className="flex items-center gap-2"><CalendarDays className="size-4 text-[#315b49]" />Flexible departures</span></div></div></div><aside className="surface h-fit p-6 lg:sticky lg:top-24"><p className="text-xs font-bold uppercase tracking-[.14em] text-[#8b5e2f]">From</p><p className="mt-1 font-display text-4xl text-[#263e34]">{formatKes(tour.price)}</p><p className="text-xs text-[#7c7265]">per person, indicative price</p><button onClick={() => document.getElementById("inquiry")?.scrollIntoView({ behavior: "smooth" })} className="mt-6 w-full rounded-full bg-[#315b49] py-3 text-sm font-bold text-white transition hover:bg-[#254939]">Send inquiry</button><div className="mt-5 border-t border-[#e8decc] pt-5"><p className="text-sm font-bold text-[#263e34]">Hosted by {tour.operator}</p><p className="mt-1 text-xs leading-5 text-[#6b6255]">Verified operator profile · Enquiries are shared directly for confirmation.</p><button onClick={() => setLocation("/operators")} className="mt-3 flex items-center gap-1 text-xs font-bold text-[#315b49]">Meet the operator <ChevronRight className="size-3" /></button></div></aside></div></section><section className="container grid gap-8 pb-20 lg:grid-cols-[1.35fr_.75fr]"><div className="space-y-8"><section className="surface p-6 md:p-8"><p className="eyebrow">A day-by-day rhythm</p><h2 className="mt-2 font-display text-4xl text-[#263e34]">The itinerary</h2><div className="mt-7 space-y-0">{tour.itinerary.map((day, index) => <div key={day.day} className="grid grid-cols-[72px_1fr] gap-4 border-b border-[#eadfce] py-5 last:border-0"><p className="text-xs font-bold uppercase tracking-[.12em] text-[#a07136]">{day.day}</p><div><h3 className="font-bold text-[#263e34]">{day.title}</h3><p className="mt-2 text-sm leading-6 text-[#6b6255]">{day.detail}</p></div>{index < tour.itinerary.length - 1 && <div />}</div>)}</div></section><section className="grid gap-5 md:grid-cols-2"><div className="surface p-6"><h2 className="font-display text-3xl text-[#263e34]">Included</h2><ul className="mt-5 space-y-3">{tour.inclusions.map((item) => <li key={item} className="flex gap-2 text-sm text-[#62594d]"><Check className="mt-0.5 size-4 shrink-0 text-[#315b49]" />{item}</li>)}</ul></div><div className="surface p-6"><h2 className="font-display text-3xl text-[#263e34]">Not included</h2><ul className="mt-5 space-y-3">{tour.exclusions.map((item) => <li key={item} className="flex gap-2 text-sm text-[#62594d]"><X className="mt-0.5 size-4 shrink-0 text-[#a07136]" />{item}</li>)}</ul></div></section><section className="surface p-6"><div className="flex gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-full bg-[#e8f0e6] text-[#315b49]"><ShieldCheck className="size-5" /></span><div><h2 className="font-display text-3xl text-[#263e34]">Before you go</h2><p className="mt-2 text-sm leading-6 text-[#665d51]">Your host will confirm exact departure timing, pickup details, accessibility needs, cancellation terms, and availability once they receive your inquiry.</p></div></div></section><section className="surface p-6"><div className="flex gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-full bg-[#f6ead1] text-[#967142]"><MessageCircle className="size-5" /></span><div><p className="eyebrow">Verified guest feedback</p><h2 className="mt-2 font-display text-3xl text-[#263e34]">Reviews &amp; ratings</h2><p className="mt-2 text-sm leading-6 text-[#665d51]">No verified guest feedback has been published for this itinerary yet. Tulia only displays ratings and reviews once they are confirmed, so travelers can plan with context they can trust.</p></div></div></section></div><section id="inquiry" className="surface h-fit p-6 lg:sticky lg:top-24"><p className="eyebrow">Start a conversation</p><h2 className="mt-2 font-display text-4xl text-[#263e34]">Plan this trip.</h2><p className="mt-2 text-sm leading-6 text-[#6b6255]">A few details help {tour.operator} offer the right next step.</p><form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-xs font-bold text-[#5e554a]">Your name<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} className="mt-1.5 w-full rounded-xl border border-[#e4dac8] bg-[#fffdf8] px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-[#d5a44f]" /></label><label className="block text-xs font-bold text-[#5e554a]">Email address<input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} className="mt-1.5 w-full rounded-xl border border-[#e4dac8] bg-[#fffdf8] px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-[#d5a44f]" /></label><div className="grid grid-cols-2 gap-3"><label className="block text-xs font-bold text-[#5e554a]">Travelers<select value={form.travelers} onChange={(e) => setForm({ ...form, travelers: e.target.value })} className="mt-1.5 w-full rounded-xl border border-[#e4dac8] bg-[#fffdf8] px-3 py-2.5 text-sm outline-none"><option>1</option><option>2</option><option>3</option><option>4</option><option>5</option><option>6</option></select></label><label className="block text-xs font-bold text-[#5e554a]">Travel date<input type="date" value={form.date} onChange={(e) => setForm({ ...form, date: e.target.value })} className="mt-1.5 w-full rounded-xl border border-[#e4dac8] bg-[#fffdf8] px-3 py-2.5 text-sm outline-none" /></label></div><label className="block text-xs font-bold text-[#5e554a]">Anything else?<textarea value={form.message} onChange={(e) => setForm({ ...form, message: e.target.value })} rows={3} placeholder="Tell us what would make this feel right." className="mt-1.5 w-full resize-none rounded-xl border border-[#e4dac8] bg-[#fffdf8] px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-[#d5a44f]" /></label><button disabled={inquiry.isPending} className="w-full rounded-full bg-[#315b49] py-3 text-sm font-bold text-white transition hover:bg-[#254939] disabled:opacity-60">{inquiry.isPending ? "Sending inquiry…" : "Send inquiry"}</button></form></section></section></main></div>;
}
